# Ah, `nix` the Experimental

 * `flake` - The unit of code.
 * `registry` - The DNS of sorts.
 * `store` - The on disk collection of things.
 * `profile` - The users environment.
 * `develop` - Open shell environment of a flake.

Seems simple, no?

