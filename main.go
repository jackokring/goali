package main

import goali "github.com/jackokring/goali/pkg"

func main() {
	// The bulk of the package utility and library code
	// Allows import of all from package goali
	// The binary name prevents naming goali, so ./pkg
	goali.Goali()
}
